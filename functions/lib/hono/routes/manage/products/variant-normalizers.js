/**
 * Re-export from services/_shared/ — canonical location.
 * 保留此文件以兼容路由层的相对导入。
 */
export {
  normalizeVariantExternalCodes,
  normalizeVariantDimensionKeys,
} from '../../../../../services/_shared/variant-normalizers.js';
