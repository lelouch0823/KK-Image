<view class="metric-card metric-card--{{metric && metric.tone}}">
  <text class="metric-card__label">{{metric && metric.label}}</text>
  <text class="metric-card__value">{{metric && metric.value}}</text>
  <text class="metric-card__helper">{{metric && metric.helper}}</text>
</view>
